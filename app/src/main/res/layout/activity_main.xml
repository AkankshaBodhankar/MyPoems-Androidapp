<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:background="@color/backgroundApp" >

    <TextView
        android:id="@+id/mypoems_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_margin="@dimen/layoutMarginList"
        android:text="@string/mypoems"
        android:textSize="@dimen/titleTextViewSize"
        android:textStyle="bold"
        android:textColor="@color/headTextViewcolor"
        android:textAppearance="?android:attr/textAppearanceMedium"
        />

    <ExpandableListView
        android:id="@+id/explist"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor ="@color/headTextViewcolor"
        android:divider="@drawable/list_divider"
        android:dividerHeight="4px"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/mypoems_tv"
        />

</LinearLayout>
